--!strict
--services
--packages
local Elevation = require(script:WaitForChild("Elevation"))
local MaterialColor = require(script:WaitForChild("MaterialColor"))
local Motions = require(script:WaitForChild("Motions"))
local Typography = require(script:WaitForChild("Typography"))
--modules
--types
--constants
--remotes
--variables
--references
--local functions
--class
return {
    Elevation = Elevation,
    MaterialColor = MaterialColor,
    Motions = Motions,
    Typography = Typography
}